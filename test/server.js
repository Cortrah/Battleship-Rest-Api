'use strict';

const Assert = require('assert');
const Lab = require('lab');

const Server = require('../server');

const lab = exports.lab = Lab.script();

lab.experiment('creates a new match with two players', () => {

  lab.test('- blank test', (done) => {
    done();
  });
});
